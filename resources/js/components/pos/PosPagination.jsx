// Re-exports the shared Pagination component so the POS pages
// (PrescriptionSales / RetailOTCSales) render the exact same
// pagination layout, spacing and styling as every other page.
export { default } from '../Pagination';
